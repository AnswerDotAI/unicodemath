[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "unicodemath"
dynamic = ["version"]
description = "UnicodeMath and AsciiMath linear-format math parser with pluggable output backends"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "Apache-2.0" }
authors = [{ name = "unicodemath contributors" }]
classifiers = [
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3 :: Only",
]

dependencies = ["fastcore>=2.1.2"]

[project.optional-dependencies]
dev = [
  "fastship",
  "build",
  "twine",
]

[project.urls]
Homepage = "https://github.com/AnswerDotAI/unicodemath"

[tool.setuptools.dynamic]
version = { attr = "unicodemath.__version__" }

[tool.setuptools.packages.find]
include = ["unicodemath"]
